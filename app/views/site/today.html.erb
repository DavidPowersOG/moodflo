<h3><%= Time.now.strftime("%b %d, %Y") %></h3>

<% @lists.each do |list| %>
	<h4><%=list.title %></h4>
	<ul>
	<% list.activities.each do |activity| %>
		<li><%=activity.title %></li>

	<% end %>
	</ul>
<% end %>
